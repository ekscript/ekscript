import test from 'ava';

// import { getFile } from '../../utils/fileOps';

// import Compiler from './compile';

// test('Test basic working of the compiler', (t) => {
//   new Compiler({
//     filePath: 'hello.ek',
//     fileContent: getFile('./testFiles/hello.ek'),
//   });
//   t.pass('to be dealt later');

//   // compiler.parse().generateCode();

//   // const { errors, warnings } = compiler.parse();

//   // t.deepEqual(errors, []);
//   // t.deepEqual(warnings, []);
// });
test.todo('later on update this TODO');
