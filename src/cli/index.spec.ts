import test from 'ava';

// import { getFile } from '../utils/fileOps';

// test('File content is string', (t) => {
//   t.is(typeof getFile('./testFiles/hello.ek'), 'string');
//   t.is(typeof getFile('./testFiles/hello.c.txt'), 'string');
// });
//
test.todo('File content is string');
