#include<stdbool.h>
const char *concat_string_lit_int(const char *str, const size_t strLen, const int i) {
  char *newStr = (char *)malloc(sizeof(char) * (strLen + 51));
  snprintf(newStr, (strLen + 51), "%s%d", str, i);
  printf("%ld\n", strlen(newStr));
  return newStr;
}
int main(int argc, char **argv) {
  1 + ( 1 + 1.2 * ( 1 * 7 / 8 ) && 1 ) ;
  ! 1 + 1 ;
  a + 1 ;
  yo ;
  true ;
  false ;
  "hello world" ;
  'a' ;
  '' ;
  "hello1" ;
  "yot" ;
  const char* temp_1 = concat_string_lit_int( "there", 5, a);
  temp_1 ;
  "yoyo" ;
  const char* temp_2 = concat_string_lit_int( "paren", 5, (a));
  temp_2 ;
  "parentrue" ;
  free(temp_1);
  free(temp_2);
  return 0;
}
